<%= form_for(message_type) do |f| %>
  <% if message_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message_type.errors.count, "error") %> prohibited this message_type from being saved:</h2>

      <ul>
      <% message_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :NameMessage %>
    <%= f.text_field :NameMessage %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
